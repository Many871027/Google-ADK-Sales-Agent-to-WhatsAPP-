Archivos de Entorno Virtual y Dependencias de Python (Backend)
Nunca subir la carpeta del entorno virtual. Contiene paquetes que se reinstalan.
.venv/
env/
venv/

.env
.env.*
*.env
gcloud-key.json 
*.json 
.env.docker
*.env.docker
.env.docker/

Archivos de caché y compilados de Python
pycache/
*.pyc
*.pyo
*.pyd

Artefactos de distribución e instalación
build/
dist/
*.egg-info/
*.egg

Archivos Sensibles y de Configuración
¡MUY IMPORTANTE! Nunca subir archivos con claves, contraseñas o secretos.
.env
*.env

Archivos de Desarrollo de Agentes (ADK)
Sesiones de prueba guardadas localmente por adk web
*.session.json

Archivos de Frontend (React Native / Node.js)
La carpeta de dependencias de Node.js, que es muy pesada.
node_modules/

Archivos de log de NPM y Yarn
npm-debug.log*
yarn-debug.log*
yarn-error.log*

Carpetas de build generadas por Android Studio y Xcode
/android/build/
/android/app/build/
/ios/build/

Caché del bundler de React Native (Metro)
.metro-cache/

Archivos de configuración local del entorno de desarrollo de React Native
.expo/
*.jks
*.keystore

Archivos de IDE y Editores de Código
Configuraciones locales de VS Code
.vscode/*
!.vscode/settings.json
!.vscode/tasks.json
!.vscode/launch.json
!.vscode/extensions.json

Archivos del IDE JetBrains (PyCharm, WebStorm, etc.)
.idea/

Checkpoints de Jupyter Notebook
.ipynb_checkpoints

Archivos del Sistema Operativo
Archivos de metadatos de macOS
.DS_Store

Archivos de caché de miniaturas de Windows
Thumbs.db
ehthumbs.db